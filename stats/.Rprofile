## Ensure Quarto renders use the repo renv library.
## Quarto runs this stats site with working directory = `stats/`.
renv_activate <- file.path("..", "renv", "activate.R")
if (file.exists(renv_activate)) {
  source(renv_activate)
}

